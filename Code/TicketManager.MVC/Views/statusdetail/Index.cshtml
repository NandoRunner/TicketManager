@model IEnumerable<TicketManager.MVC.Models.statusdetail>

@{
    ViewBag.Title = "Postion/Result List";
}

<h2>Postion/Result List</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StatusDetailName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status.StatusName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StatusDetailName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.status.StatusName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.StatusDetailId }) |
            @Html.ActionLink("Details", "Details", new { id=item.StatusDetailId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.StatusDetailId })
        </td>
    </tr>
}

</table>
